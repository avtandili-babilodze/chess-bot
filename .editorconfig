root = true

[*]
charset = utf-8
end_of_line = lf
trim_trailing_whitespace = true
insert_final_newline = true

# Batch files need CRLF and must NOT have a UTF-8 BOM or cmd.exe breaks.
[*.{bat,cmd}]
charset = utf-8
end_of_line = crlf
